{% include [
  ('@views/teases/' ~ post.post_type ~ '-' ~ tease_options.variant ~ '.twig'),
  ('@views/teases/' ~ post.post_type ~ '.twig'),
  '@views/tease.twig'
] with {
  post,
  tease_options,
  current_page,
  theme_attr: theme_attr,
  addition_column_class
} only %}
