
export default {
  setup()  {
    // Example usecase
    //
    // acf.addAction('load_field/name=video', (field) => {
    //   field.$el.find('.hide-if-value .button').text((_idx, text) => text.replace('Image', 'Video'));
    // });
  }
};